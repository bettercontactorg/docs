.DS_Store
**/.DS_Store
.claude/settings.local.json
node_modules/
.mintlify/

# Regenerated by `mintlify dev` on every run (legacy mint.json auto-upgrade).
# mint.json stays the source of truth until we decide to migrate.
docs.json
